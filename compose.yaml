services:
  jlox-web:
    build:
      context: .
      dockerfile: Dockerfile
    image: jlox-web:latest
    container_name: jlox-web
    ports:
      - "8000:80"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
